export interface AppStoreTypes {
  isSignStatus: boolean;
  setIsSignStatus: (signValue: boolean) => void;
  // jwtToken: string | null;
  // refreshToken: string | null;
  //
  // setRefreshToken: (token: string) => void;
  // setNewToken: (newToken: string) => void;
  // setEmail: (email: string) => void;
  // login: ({ jwtToken, refreshToken }: ITokenResponse) => void;
  // logout: () => void;
}
